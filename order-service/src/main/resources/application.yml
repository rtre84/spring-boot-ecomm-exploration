kafka:
  bootstrap-servers: localhost:9092
server:
  port: 8082
spring:
  application:
    name: order-service
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  sleuth:
    sampler:
      probability: 1.0
